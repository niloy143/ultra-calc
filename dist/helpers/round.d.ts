/**
 * Removes the last decimal digit and rounds the number
 * @param num a valid sanitized number (@type String)
 */
export default function round(num: string): string;
//# sourceMappingURL=round.d.ts.map